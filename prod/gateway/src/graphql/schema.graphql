# import User from 'users.graphql'
# import Session from 'users.graphql'
# import Message from 'messages.graphql'
# import MesageInput from 'messages.graphql'

type Query {
  users: [User!]!
  session: Session!

  messages: [Message!]!
}

type Mutation {
  createUser(email: String!, password: String!): User!
  createSession(email: String!, password: String!): Session!

  createMessage(input: MesageInput): Message!
}
